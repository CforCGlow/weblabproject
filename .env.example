# Copy to .env.local and fill in real values (never commit secrets)
MONGODB_URI=mongodb+srv://<db_user>:<db_password>@<cluster-host>/ligapro?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=change_me_to_a_long_random_string
NEXT_PUBLIC_APP_NAME=SEU LigaPro
